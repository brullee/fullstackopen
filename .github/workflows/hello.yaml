name: Hello World!

on:
  push:
    branches: [main]
    paths:
      - "part11/pokedex/**"
      - ".github/workflows/hello.yml"

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello World!"
      - name: Run date
        run: date
      - name: Run ls -l
        run: ls -l
